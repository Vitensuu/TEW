namespace Game.Dungeon
{
    /// <summary>Тип двери (ТЗ «Система дверей» — Типы).</summary>
    public enum DoorType { Normal, Locked, Boss, Secret }

    /// <summary>Состояние двери (ТЗ «Система дверей» — Состояния).</summary>
    public enum DoorState { Open, Closed, Locked }
}
